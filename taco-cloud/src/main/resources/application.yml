spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/taco
    username: qq2
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  
  main:
    allow-circular-references: true